import {Dimensions, Platform} from 'react-native';
const ScreenWidth = Dimensions.get('screen').width;
const ScreenHeight = Dimensions.get('screen').height;
export {ScreenHeight, ScreenWidth, Platform};
